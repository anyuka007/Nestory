const Categories = ({ categoryName }) => {
    return (
        <div>
            Categories:
            <p>Displaying {categoryName} Items...</p>
        </div>
    );
};

export default Categories;
